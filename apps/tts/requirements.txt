# Core service
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
soundfile>=0.12.0
numpy
regex>=2024.0.0  # Unicode property classes (\p{Lu}, \p{Lo}) for sentence splitting

# OmniVoice engine (needs a CUDA GPU + torch)
# torch + torchaudio are installed separately for your CUDA version (see Dockerfile).
# They MUST match — install both from the same index, e.g.:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128  # Blackwell/50xx
omnivoice

# RVC (only needed when you replace the stub with a real converter)
# rvc-python
